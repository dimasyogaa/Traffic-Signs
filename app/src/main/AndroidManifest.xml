<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yogadimas.trafficsigns">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrafficSigns">
        <activity
            android:name=".view.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.web.WebTwoActivity" />
        <activity android:name=".view.web.WebOneActivity" />
        <activity android:name=".view.prohibition.ProhibitionActivity" />
        <activity android:name=".view.warning.WarningActivity" />
        <activity android:name=".view.command.CommandActivity" />
        <activity android:name=".view.guide.GuideActivity" />

    </application>

</manifest>